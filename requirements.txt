# Agent Harness - Python 依赖

# 核心框架
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# LLM 相关
openai>=1.0.0
tiktoken>=0.5.0

# LangChain 生态（新增）
langchain>=0.1.0
langchain-openai>=0.0.5
langchain-community>=0.0.20
langgraph>=0.0.20

# 数据库
pymysql>=1.1.0          # MySQL 驱动
cryptography>=41.0.0    # MySQL 加密支持
aiosqlite>=0.19.0       # SQLite 异步（保留兼容）

# 配置文件
PyYAML>=6.0
python-dotenv>=1.0.0

# 向量数据库（知识关联）
chromadb>=0.4.0

# 工具
python-multipart>=0.0.6
jinja2>=3.1.0
tenacity>=8.2.0         # 重试机制

# 开发工具
pytest>=7.4.0
pytest-asyncio>=0.21.0
