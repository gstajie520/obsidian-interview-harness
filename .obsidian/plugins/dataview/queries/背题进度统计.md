# 📊 Java 八股文背题进度

## 总体进度

```dataview
TABLE WITHOUT ID
  choice(熟练度 = "🟢", "🟢 已掌握", choice(熟练度 = "🟡", "🟡 需复习", choice(熟练度 = "🔴", "🔴 待学习", "⚪ 未标记"))) as 状态,
  length(rows) as 数量,
  round(length(rows) / 1346 * 100, 1) + "%" as 占比
FROM "Java八股文"
WHERE file.name != "背题进度统计"
GROUP BY 熟练度
SORT 熟练度 DESC
```

## 各模块掌握情况

```dataview
TABLE WITHOUT ID
  file.folder as 模块,
  length(rows) as 题目数,
  length(filter(rows, (r) => r.熟练度 = "🟢")) as 已掌握,
  round(length(filter(rows, (r) => r.熟练度 = "🟢")) / length(rows) * 100, 1) + "%" as 掌握率
FROM "Java八股文"
WHERE file.name != "背题进度统计"
GROUP BY file.folder
SORT 掌握率 DESC
```

## 今日待复习（基于遗忘曲线）

```dataview
TABLE WITHOUT ID
  file.link as 题目,
  熟练度,
  上次复习 as 上次,
  choice(date(today) - date(上次复习) >= dur(7 days), "⚠️ 需复习", "✅ 正常") as 状态
FROM "Java八股文"
WHERE 熟练度 = "🟡" OR 熟练度 = "🟢"
WHERE date(today) - date(上次复习) >= dur(7 days)
SORT 上次复习 ASC
LIMIT 20
```

## 最近学习的题目

```dataview
TABLE WITHOUT ID
  file.link as 题目,
  熟练度,
  上次复习,
  复习次数
FROM "Java八股文"
WHERE 上次复习
SORT 上次复习 DESC
LIMIT 10
```

## 薄弱环节（需要重点攻克）

```dataview
TABLE WITHOUT ID
  file.link as 题目,
  复习次数,
  上次复习
FROM "Java八股文"
WHERE 熟练度 = "🔴" OR (复习次数 >= 3 AND 熟练度 != "🟢")
SORT 复习次数 DESC, 上次复习 ASC
LIMIT 15
```

---

**使用说明**：
- 🟢 已掌握：可以流畅回答，7 天后复习
- 🟡 需复习：基本会答，3 天后复习
- 🔴 待学习：不会或很模糊，1 天后复习
- ⚪ 未标记：还未开始学习
